[package]
name = "fex"
version = "0.1.0"
edition = "2021"\nauthors = ["FEX Developers"]
description = "Format EXchange - convert binary files and JSON-defined formats"
license = "GPL-3.0-or-later"
repository = "https://github.com/your/repo"

[dependencies]
anyhow = "1.0"
clap = { version = "4.4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
